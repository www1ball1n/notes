name: Disabled Biome workflow

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Inform
        run: echo "This workflow is disabled. Deployment is handled by deploy.yml."
