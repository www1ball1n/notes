# This workflow is intentionally disabled.
# GitHub Pages deployment is handled by deploy.yml.

name: Disabled Astro Pages workflow

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Inform
        run: echo "This workflow is disabled. Use deploy.yml for GitHub Pages deployments."
